import { PrivacyView } from './privacy-view';

export const metadata = {
  title: 'Privacy Policy · Privacidad — Pangea Labs',
  description: 'What data Pangea Wallet handles — and, above all, what it doesn’t (EN/ES).',
};

export default function Privacy() {
  return <PrivacyView />;
}
